import { LiveMapChart } from ".";

export default {
  title: "Components/LiveMapChart",
  component: LiveMapChart,
  argTypes: {
  },
};

export const Default = {
  args: {
    className: {},
    liveFiresClassName: {},
  },
};
